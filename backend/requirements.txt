# FastAPI and Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
websockets==12.0
python-multipart==0.0.6

# Databricks SDK
databricks-sdk==0.18.0

# Cost-Effective AI/ML Libraries
transformers==4.36.0
torch==2.1.2 --index-url https://download.pytorch.org/whl/cpu
# Note: Using CPU-only torch to reduce costs

# Cost-Effective TTS (FREE)
edge-tts==6.1.9

# Cost-Effective Emotion Detection (lightweight)
# Uses distilroberta - runs on CPU efficiently

# Audio Processing (lightweight)
pydub==0.25.1
soundfile==0.12.1

# Utilities
python-dotenv==1.0.0
pydantic==2.5.3
pydantic-settings==2.1.0
httpx==0.26.0
numpy==1.24.3
aiofiles==23.2.1

# Optional: Whisper for fallback ASR (use tiny model)
# openai-whisper==20231117  # Uncomment if needed
